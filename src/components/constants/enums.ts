export enum RESOURCESTATUS {
  AVAILABLE = "Available",
  VACATION = "Vacation",
  BUSY = "Busy",
  COMPLETED = "Completed",
}

export enum PROJECTSTATUS {
  ACTIVE = "Active",
  CLOSED = "Closed",
}

export enum ACTION {
  ALLOW = "Allow",
  RESTRICT = "Restrict",
  DELETE = "Delete",
}
